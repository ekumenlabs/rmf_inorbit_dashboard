name: build-dashboard-workflow
on:
  ## push:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-dashboard:
    runs-on: ubuntu-latest
    container:
      image: osrf/ros:humble-desktop
    env:
      DEBIAN_FRONTEND: noninteractive
      TERM: linux
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: python3 -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: cd rmf_inorbit_dashboard/client/web-dashboard
             npm install
      - uses: ros-tooling/setup-ros@v0.6
        with:
          required-ros-distributions: humble
      - uses: ros-tooling/action-ros-ci@v0.3
        with:
          package-name: rmf_inorbit_dashboard
          target-ros2-distro: humble
      ## - run: rosdep update
      ## - run: mkdir src
      ## - run: rosdep install --from-paths src --rosdistro humble -y --ignore-src


      ## - run: cd ../../../
      ## - run: source /opt/ros/humble/setup.bash
      ##     colcon build --packages-up-to rmf_inorbit_dashboard
